import { SuspensionAccessGuard } from './suspension-access.guard';

describe('SuspensionAccessGuard', () => {
  it('should be defined', () => {
    expect(new SuspensionAccessGuard()).toBeDefined();
  });
});
